# Contracts Workspace

This workspace hosts shared cross-runtime contracts.

For P1-S2 it only exposes a small placeholder module so the backend and mobile workspaces can resolve a shared package through npm workspaces. Domain schemas and validation belong to later plan steps.